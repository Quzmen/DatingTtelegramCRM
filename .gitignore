# Личные данные: БД контактов/переписки, сессия Telegram (по сути ключ
# доступа к аккаунту), кэш скачанных медиа. Никогда не должны попасть
# в git — переносятся на сервер отдельно, см. README_DEPLOY.md.
data/

# Секреты / локальные настройки
.env

# Python / IDE
__pycache__/
*.pyc
.venv/
.idea/
